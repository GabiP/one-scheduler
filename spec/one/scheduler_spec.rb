require 'spec_helper'

describe One::Scheduler do
  it 'has a version number' do
    expect(One::Scheduler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
